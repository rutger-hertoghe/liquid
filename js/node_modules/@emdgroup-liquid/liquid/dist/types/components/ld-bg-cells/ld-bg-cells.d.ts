export declare type CellType = 'bioreliance' | 'f' | 'functional' | 'hexagon' | 'millipore' | 'milliq' | 'o' | 'organic' | 'plastic' | 'qa-x2f-qc' | 'safc' | 'sigma-aldrich' | 'supelco' | 'synthetic' | 't' | 'technical' | 'tile';
/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 * @part layer - Element containing the cells pattern
 * @part secondary-layer - Element containing the second cells pattern
 */
export declare class LdBgCells {
  /** Cells pattern */
  type: CellType;
  /** Use 3 color layers */
  threeLayers: boolean;
  render(): any;
}
