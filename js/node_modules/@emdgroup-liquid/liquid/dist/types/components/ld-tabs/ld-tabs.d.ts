import { EventEmitter } from '../../stencil-public-runtime';
/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 */
export declare class LdTabs {
  el: HTMLElement;
  /**
   * Emitted with the id of the selected tab.
   */
  ldtabchange: EventEmitter<string>;
  private idDescriber;
  private updateTabs;
  private updateTabPanels;
  private handleLdtabselect;
  /** Set selected tab to a certain index */
  switchTab(identifier: number | string): Promise<void>;
  componentDidRender(): void;
  render(): any;
}
