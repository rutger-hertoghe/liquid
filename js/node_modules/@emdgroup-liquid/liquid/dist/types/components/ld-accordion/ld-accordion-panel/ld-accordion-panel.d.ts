import { EventEmitter } from '../../../stencil-public-runtime';
/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 */
export declare class LdAccordionPanel {
  el: HTMLElement;
  private contentRef;
  expanded: boolean;
  initialized: boolean;
  transitionEnabled: boolean;
  maxHeight: number;
  resizeObserver: ResizeObserver;
  innerPanelExpanding: boolean;
  /** Emitted on accordion panel max-height change. */
  ldaccordionmaxheightchange: EventEmitter<number>;
  /**
   * @internal
   * Updates expanded state.
   */
  setExpanded(expanded: boolean): Promise<void>;
  private applyMaxHeight;
  private handleInnerMaxHeightChange;
  private onTransitionEnd;
  componentDidLoad(): void;
  disconnectedCallback(): void;
  render(): any;
}
