/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 */
export declare class LdSidenavSubnav {
  el: HTMLElement;
  private sidenav;
  private scrollerRef;
  private bgRef;
  /**
   * @internal
   * Internal prop indicating that the subnav is about to become active which
   * may happen before a transition finishes after which it actually becomes active.
   */
  activeBeforeTransition: boolean;
  /**
   * @internal
   * Internal prop indicating that the subnav is either ancestor of the
   * currently visible subnav or the currently visible subnav itself.
   */
  active: boolean;
  /**
   * @internal
   * Internal prop indicating that the subnav is ancestor of the
   * currently visible subnav.
   */
  ancestor: boolean;
  /** Used in the ld-sidenav-back component to display parent nav label. */
  label: string;
  hasParentSubnav: boolean;
  /** Scrolls the subnav scroll container to the top. */
  scrollToTop(smoothly?: boolean): Promise<void>;
  onActiveChange(active: any): void;
  onActiveBeforeTransitionChange(activeBeforeTransition: any): void;
  private updateBackground;
  private toggleVisibilityOnHidableContent;
  handleSidenavCollapsedChange(ev: CustomEvent<{
    collapsed: boolean;
    fully: boolean;
  }>): void;
  handleSidenavBreakpointChange(ev: CustomEvent<boolean>): void;
  componentWillLoad(): void;
  render(): any;
}
