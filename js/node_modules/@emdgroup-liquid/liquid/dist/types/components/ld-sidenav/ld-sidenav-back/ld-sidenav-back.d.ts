import { EventEmitter } from '../../../stencil-public-runtime';
/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 */
export declare class LdSidenavBack {
  el: HTMLElement;
  private sidenav;
  /** Emitted on click. */
  ldSidenavBack: EventEmitter;
  /** Used as aria-label for the back button */
  backLabel: string;
  parentLabel: string;
  rounded: boolean;
  sidenavCollapsed: boolean;
  sidenavClosable: boolean;
  handleSidenavCollapsedChange(ev: CustomEvent<{
    collapsed: boolean;
    fully: boolean;
  }>): void;
  handleSidenavBreakpointChange(ev: any): void;
  /**
   * @internal
   * Updates the label of the back button.
   */
  updateLabel(text?: string): Promise<void>;
  private onClick;
  private onKeyDown;
  componentWillLoad(): void;
  render(): any;
}
