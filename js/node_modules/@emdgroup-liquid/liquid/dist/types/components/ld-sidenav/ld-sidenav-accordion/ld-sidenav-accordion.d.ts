import { EventEmitter } from '../../../stencil-public-runtime';
/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 */
export declare class LdSidenavAccordion {
  el: HTMLElement;
  private sidenav;
  private sectionRef;
  private panelRef;
  expandOnSidenavExpansion: boolean;
  inAccordion: boolean;
  noIcon: boolean;
  rounded: boolean;
  sidenavClosable: boolean;
  sidenavCollapsed: boolean;
  transitionsEnabled: boolean;
  /** Indicates that the accordion section is expanded. */
  expanded?: boolean;
  /**
   * Set to false to make the accordion collapse on sidenav collapse
   * or slide change.
   */
  preserveState: boolean;
  /**
   * Split the accordion toggle in two parts with the second part containing
   * the caret icon and being responsible for expanding / collapsing
   * the accordion panel.
   */
  split?: boolean;
  /** Emitted on accordion expand or collaps transition end. */
  ldSidenavAccordionTransitionEnd: EventEmitter;
  handleSidenavBreakpointChange(ev: CustomEvent<boolean>): void;
  handleSidenavSliderChange(ev: CustomEvent<boolean>): void;
  handleSidenavCollapsedChange(ev: CustomEvent<{
    collapsed: boolean;
    fully: boolean;
  }>): void;
  handleExpandedChange(expanded: any): void;
  private onTransitionEnd;
  componentWillLoad(): void;
  render(): any;
}
