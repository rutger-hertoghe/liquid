/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 * @part tag - Actual label element
 */
export declare class LdLabel implements ClonesAttributes {
  el: HTMLLabelElement;
  private attributesObserver;
  /** Align input message with input position. */
  alignMessage: boolean;
  /** Relative position to labeled element. Default is top. */
  position: 'left' | 'right';
  /** Size of the label. Default is small. */
  size: 'm';
  clonedAttributes: any;
  private handleClick;
  componentWillLoad(): void;
  disconnectedCallback(): void;
  render(): any;
}
