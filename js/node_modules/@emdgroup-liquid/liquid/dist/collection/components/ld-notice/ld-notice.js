import { Component, h, Prop, Host, Element } from '@stencil/core';
/**
 * @virtualProp ref - reference to component
 * @virtualProp {string | number} key - for tracking the node's identity when working with lists
 * @part headline - `ld-typo` element used for the headline
 * @part icon - Image tag used for the icon
 */
export class LdNotice {
  constructor() {
    /** Mode of the notice. */
    this.mode = 'info';
  }
  render() {
    return (h(Host, { class: `ld-notice ld-notice--${this.mode}` },
      h("slot", { name: "custom-icon" },
        h("ld-icon", { class: "ld-notice__icon", name: this.mode === 'success'
            ? 'ld-input-message-success'
            : 'ld-input-message-info', part: "icon", size: "lg" })),
      this.headline && (h("ld-typo", { class: "ld-notice__headline", variant: "h4", tag: "p", part: "headline" }, this.headline)),
      h("slot", null)));
  }
  static get is() { return "ld-notice"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() { return {
    "$": ["ld-notice.css"]
  }; }
  static get styleUrls() { return {
    "$": ["ld-notice.css"]
  }; }
  static get assetsDirs() { return ["assets"]; }
  static get properties() { return {
    "headline": {
      "type": "string",
      "mutable": false,
      "complexType": {
        "original": "string",
        "resolved": "string",
        "references": {}
      },
      "required": false,
      "optional": true,
      "docs": {
        "tags": [],
        "text": "Headline of the notice."
      },
      "attribute": "headline",
      "reflect": false
    },
    "mode": {
      "type": "string",
      "mutable": false,
      "complexType": {
        "original": "'error' | 'info' | 'warning' | 'success'",
        "resolved": "\"error\" | \"info\" | \"success\" | \"warning\"",
        "references": {}
      },
      "required": false,
      "optional": false,
      "docs": {
        "tags": [],
        "text": "Mode of the notice."
      },
      "attribute": "mode",
      "reflect": false,
      "defaultValue": "'info'"
    }
  }; }
  static get elementRef() { return "hostElement"; }
}
