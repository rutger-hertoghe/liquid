import type { Components, JSX } from "../types/components";

interface LdCircularProgress extends Components.LdCircularProgress, HTMLElement {}
export const LdCircularProgress: {
  prototype: LdCircularProgress;
  new (): LdCircularProgress;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
